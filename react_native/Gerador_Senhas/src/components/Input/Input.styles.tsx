import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {},
    Input: {
        borderColor: 'black',
        borderWidth: 2,
        borderRadius: 10,
        width: 300,
        height: 50,
        marginBottom: 20,
        paddingLeft: 10,
    },
});

export default styles;